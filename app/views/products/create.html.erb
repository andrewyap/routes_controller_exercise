<h2>Create view</h2>
<% product = Product.new(params[:product]) %>
<% if product.save %>
	<h4> Congrats you created a new product!</h4>
<% else %>
	<h4> Your product was *NOT* saved! </h4>
	<%= product.errors.full_messages %>
	<p>Please go back in your browser and fix the problem</p>
<% end %>

